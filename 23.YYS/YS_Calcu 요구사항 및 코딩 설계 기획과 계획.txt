YS_Calcu 요구사항 및 코딩 설계 기획과 계획

요구사항
1. 사칙연산 + - * /
2. BicDecimal 사용
3. 각 연산은 함수로 구현 (함수 이름은 본인이 만들어 보기)
4. 연산 결과는 main 함수에서 콘솔에 출력하기
5. 입력은 Scanner 클래스 사용

=======

1. YS_Class 클래스 만들고 기본사칙연산, 
BicDecimal 할 함수 만들기

2. 어떤 값이 들어왔을 때, 어떻게 처리 할것인지 정하기
	- 사용자가 실수로 엔터를 쳐서 아무값도 입력하지 않았을 경우
	- 문자숫자를 쳐야 하는데, 일이삼 혹은 onetwothree 등과 같이
	- 잘못 입력했을 경우 어떻게 처리할 것인가?
	- 사용자가 더하기 눌렀을 때
	- 사용자가 빼기 눌렀을 때
	- 사용자가 곱하기 눌렀을 때
	- 사용자가 나누기 눌렀을 때
	- 두 수를 잘 입력했는데, +-*/이 아닌 %^&#$와 같이 잘못입력하면
	어떻게 처리할 것인가?

3. 기본 사칙연산함수 만들기

- 함수의 리턴형 키워드를 뭘로 할 것인지?
- 매개변수 어떻게 받을 것인지?
- 리턴값 어떻게 출력시킬 것인지
- 사용할 변수와 함수 내에 지역변수 초기화 및 사용은
- 어떻게 할 것인지?
- 계산과정은 어떻게 처리 할 것인지?

4. BicDecimal 클래스를 사용하여 BicDecimal 할 함수에서
사칙연산 진행

- String 값을 받아서 BicDecimal 클래스의 참조변수로 선언
