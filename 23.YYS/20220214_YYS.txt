20220214 YYS.txt

자바
---

Q1. 자바로 개발할 때 필요한 4가지 도구?

JDK, 에디터, 콘솔창(MS-DOS), API

Q2. javac와 java의 차이는?

javac는 컴파일러로 사람이 인지하는 코드로 작성해서
컴퓨터가 알아볼 수 있게 하는 것(Convert 소스코드 to 바이트코드)
+ 자바 소스를 자바의 규칙에 잘 맞게 작성했는지 검사 (규칙을 잘 지켰으면 JVM이 바이트 코드를 한든다.)

java는 jvm이 바이트코드를 cpu하고 통신해서 결과를 만드는 것

Q3. API 구성원은?

rt.jar, src.jar에 jdk 1.8 자바소스(클래스)까 8천여개가 있다.
클래스는 자바 프로그램의 최소 단위이다.

각각 파일을 뜯어서 볼 수 없기 때문에 개발자들이 문서를 참조해서
편하게 개발하라고 온라인에 API로 제공하고 있다.

Q4. 키워드 vs 연산자 vs 식별자 vs 빌트인 클래스의 차이를
말하고, 각각 해당하는 단어를 3개 이상씩 말해보시오.

키워드는 기능이 있는 영어 단어로, 자바 엔진에서 읽어서 일을 시키는 명령어이다.

연산자는 연산자 좌우로 숫자를 비교하는 단어이다. 식별자는 개발자가 일정 규칙에 맞춰 마음대로 만들어서 사용한다.

빌트인 클래스는 java 컴파일 및 실행시 사용되는 java의 패키지에 있는 클래스이다.

===
키워드
===

package import class public static new

void byte char short int long float double

if else for while do while break boolean continue

== 아직 안 배운 내용 (지금은 몰라도 됨) ==

abstract assert case catch const enum....


자바에서 상속

=====
객체 지향 프로그래밍 : Object Oriented Programming
=====

미리 만들어진 프로그램 또는 클래스

자식이 부모에게서 상속받아 사용

자식은 부모가 가지고 있는 내용을 전부 물려받아 사용 할 수 있음.

===
자바에서 빌트인 클래스의 최고 우두머리는 java.lang.Object 클래스이다.
사용자 정의 클래스는 빌트인 클래스를 가지고 만든 것이다.
java.lang.Object 클래스를 상속해서 만든다.
===

자바 프로그램을 구성하는 단위는 "클래스"이다.

클래스는 변수와 함수로 이루어져 있다. 클래스에서 클래스로
상속이 가능하다. 클래스에서 상속 관계를 가지면 부모 클래스, 자식 클래스가 성립된다.

상속을 해주는 클래스를 부모 클래스라고 하고,

상속을 받는 클래스를 자식 클래스라고 부른다.

자바에서 상속 기능을 하는 키워드??

extends 클래스 상속할 때 사용하는 키워드

implements 인터페이스를 상속할 때 사용하는 키워드

=============== 왕 중 요 ==============
객체 지향 프로그래밍 Object Oriented Programming

Java

1. All Object

2. java.lang.Object 클래스가 루트이면서
	모든 클래스(빌트인 클래스, 사용자 정의 클래스)에게 상속되어 있다.

	"내가 파평 윤씨라면 파평 윤씨의 시조의 개념이라고 볼 수 있다."

3. 클래스를 사용하려면, (클래스를 인스턴스, 클래스를 상속하려면,
	클래스를 메모리에 올리려면) 제일 먼저 생성자를 찾는다.
	생성자는 클래스를 사용하려고 할 때, 제일 먼저 멤버 필드를 초기화한다.







